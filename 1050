#include <stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	double num[100001];
	double sum=0.0;
	for(int i=1;i<=n;i++)
	{
		scanf("%lf",&num[i]);
		sum=sum+num[i]*i*(n-i+1);
	}
	printf("%.2f",sum);
	return 0;
}
